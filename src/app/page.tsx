import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <>
      <Button
        variant={"secondary"}
        className="bg-primary lg:text-lg"
      >
        Click me
      </Button>
    </>
  );
};

export default Home;
